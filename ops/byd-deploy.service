[Unit]
Description=build-your-deck pull-based deploy (DRIFT §7)
After=docker.service network-online.target
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=%DIR%
EnvironmentFile=%DIR%/.env
ExecStart=%DIR%/ops/deploy.sh
